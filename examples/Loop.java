class Main {
    public static void main(String[] argv) {
        int[] array = { 0, 1, 2 };

        for (int i = 0; i < array.length; i++) {
            assert array[i] == i : "array[i] != i";

            //if (i == array.length) {
            //    break;
            //} else { ; }
            //;
        }
    }
}
